/*
 * ---------------------------------------------------
 *  Nextflow config file for running tests on a laptop
 *  or other local device
 * ---------------------------------------------------
 */

docker.enabled = true
//docker.runOptions = '-u \$(id -u):\$(id -g)'

params {
  max_memory = 8.GB
  max_cpus = 4
  max_time = 2.h

  //seurat_RNA = "/Users/hamrude/dev/repos/atac_neural_plate_border/output/NF-scRNAseq/input/cell_cycle_data.RDS"
}

